import { dict, isDict, Dict } from './dict';
import { val, isVal, Value } from './value';
import { effect } from './effect';

export {
  dict,
  val,
  isVal,
  isDict,
  effect
};

export type {
  Dict,
  Value,
}
